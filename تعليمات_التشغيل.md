# نظام تتبع نشاط الطلاب — تعليمات التشغيل

## الملفات
| الملف | وظيفته |
|---|---|
| `Install.ps1` | يركّب كل حاجة مرة واحدة (شغّله أول واحد) |
| `StartupLogger.ps1` | يسجل فتح/قفل البرامج كل 15 دقيقة |
| `DailySummary.ps1` | يعمل ملخص نصي آخر كل يوم |
| `WeeklyArchive.ps1` | يضغط اللوج الأقدم من أسبوع |
| `BackupLogs.ps1` | ينسخ اللوج لمسار خارجي (فلاشة) |
| `Uninstall.ps1` | يشيل كل الـ Tasks لو حبيت ترجع للوضع الطبيعي |

## خطوات التركيب على كل لابتوب

1. **انسخ الفولدر كله** (`ActivityLogs` بكل ملفاته) على أي مكان على الجهاز — مثلاً سطح المكتب مؤقتًا.
2. **افتح PowerShell كـ Administrator**:
   - دوس Start، اكتب PowerShell، دوس كليك يمين → **Run as Administrator**.
3. لو ظهرلك خطأ إن السكريبتات ممنوعة، شغّل السطر ده الأول:
   ```powershell
   Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
   ```
4. روح لمكان الفولدر وشغّل التركيب:
   ```powershell
   cd "C:\path\to\ActivityLogs"
   .\Install.ps1
   ```
5. لما يقولك **"تم التركيب بنجاح"** يبقى خلاص، النظام شغال في الخلفية بالكامل ومفيش أي نافذة أو إشعار هيظهر للطالب.

## فين هتلاقي اللوج
كل حاجة بتتسجل في:
```
C:\ActivityLogs
```
الفولدر ده **مخفي** (Hidden + System) عشان الطالب العادي مايلاقيهوش بسهولة. عشان تشوفه إنت:
- افتح File Explorer → تبويب **View** → فعّل **Hidden items**.

جوه الفولدر هتلاقي:
- `process_log_2026-08-23.csv` → لوج كل يوم لوحده (وقت فتح/قفل كل برنامج)
- `summary_2026-08-23.txt` → ملخص مبسط لعدد مرات فتح كل برنامج
- `Archive\archive_2026-08-30.zip` → أرشيف أسبوعي للملفات القديمة

## المراجعة الدورية
- افتح ملفات `summary_*.txt` للمراجعة السريعة اليومية (نص عادي، سهل القراءة).
- افتح `process_log_*.csv` بإكسل لو عايز تفاصيل كاملة بالوقت والأمر الكامل.
- وصّل فلاشة فاضية بالمسار اللي حددته في `BackupLogs.ps1` (افتراضيًا `D:\ActivityBackup`) وسيبها شوية، النظام هينسخلها اللوج تلقائي أول ما يلاقيها متوصلة.

## لو عايز تلغي النظام من جهاز معين
```powershell
cd "C:\ActivityLogs"
.\Uninstall.ps1
```

## ملاحظة مهمة
- محتاج صلاحية **Administrator** مرة واحدة بس وقت التركيب (`Install.ps1`)، بعد كده كل حاجة شغالة تلقائي بصلاحيات SYSTEM من غير أي تدخل.
- غيّر مسار الفلاشة في `BackupLogs.ps1` (`$backupPath`) لو مختلف عندك قبل التركيب.
